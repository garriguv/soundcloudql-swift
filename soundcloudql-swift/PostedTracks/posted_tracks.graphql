query UserProfileQuery($id: ID!, $limit: Int!, $next: String) {
  user(id: $id) {
    postedTracksCollection(limit: $limit, next: $next) {
      collection {
        ...SmallTrack
      }
      next
    }
  }
}

fragment SmallTrack on Track {
  id
  title
  permalinkUrl
  artworkUrl
}
