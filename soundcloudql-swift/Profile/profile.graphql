query UserProfileQuery($id: ID!) {
  user(id: $id) {
    id
    username
    avatarUrl
    permalinkUrl
    city
    postedTracksCollection(limit: 10) {
      collection {
        ...SmallTrack
      }
      next
    }
    likedTracksCollection(limit: 10) {
      collection {
        ...SmallTrack
      }
      next
    }
  }
}

fragment SmallTrack on Track {
  id
  title
  permalinkUrl
}
